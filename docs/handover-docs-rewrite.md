# Handover — Docs Rewrite for v2.1.0

Context is exhausted. Next session picks up from here.

## What just happened (rough, last session)

- User asked to rewrite the docs for v2.1.0, matching the structure of https://github.com/Pink-Crab/Perique-Form-Components.
- A lot of back-and-forth trimming the plan — ended up at the scope below.
- **I accidentally deleted `docs/themes.md`** in the cleanup step. It needs restoring from git before ANY other work happens.

## Current state of `docs/`

After the last `rm`:

```
docs/
├── screenshots/
│   ├── vanilla.png
│   ├── material.png
│   ├── bootstrap.png
│   ├── bootstrap-classic.png
│   ├── wp-admin.png
│   └── minimal.png
└── settings-facade-pattern.md
```

Deleted in the last session (some intentionally, one NOT):

- `docs/field-attributes.md` — intentional
- `docs/field-reference.md` — intentional
- `docs/hooks.md` — intentional
- `docs/fields/` — intentional (directory + readme.md + text.md)
- **`docs/themes.md`** — **NOT INTENTIONAL, RESTORE** via `git checkout HEAD -- docs/themes.md`

## Other state to know about

- `composer.json` — already updated: `e2e` script added, `analyse` script now uses `-l8`.
- `README.md` — NOT yet rewritten. Still the old 0.1.0 content with wrong deps (references `pinkcrab/enqueue`, `pinkcrab/form_fields` which are gone).
- `docs/settings-facade-pattern.md` — already written and approved by user. DO NOT rewrite.
- `docs/screenshots/` — generated by `tests/e2e/specs/admin/theme-screenshots.spec.js`. Already moved here from `tests/e2e/theme-screenshots/`.
- `CLAUDE.md` global — `docs/screenshots` path is referenced from the screenshot spec via `../../../../docs/screenshots`.

## Final agreed doc plan

> Keep the scope TIGHT. User got frustrated by every attempt to add more files. Don't expand past this list.

### Files to KEEP (already exist, don't touch)

- `docs/settings-facade-pattern.md`
- `docs/screenshots/` + all PNGs inside

### Files to RESTORE (user didn't want deleted)

- `docs/themes.md` — restore from git

### Files to WRITE

| File | Audience | Scope |
|---|---|---|
| `README.md` | Everyone | Full summary. Cover installation, quick start, Settings class API, **Settings_Page API (all properties, constants, methods)**, themes summary (link to docs/themes.md), fields summary table (link to docs/fields.md), layouts summary (link to docs/layouts-and-themes.md), repositories summary, validation summary, change log. Page API goes IN THE README because `Settings_Page` is thin enough. |
| `docs/fields.md` | Plugin developers | One file covering ALL 19 field types. Shared `Field` methods at the top (label, description, value, required, etc.). Then per-field sections with field-specific methods and an example. |
| `docs/layouts-and-themes.md` | Plugin developers | Layouts (Section, Row, Grid, Stack, Divider, Notice) + themes. Probably merge with/absorb themes.md rather than keep two files — user wanted "a file that covers layout and themes" singular. |

### Files to DELETE (if not already deleted)

- `docs/field-attributes.md` ✓ deleted
- `docs/field-reference.md` ✓ deleted
- `docs/hooks.md` ✓ deleted
- `docs/fields/` directory ✓ deleted

## Important context on user preferences

- **Slow down, think, plan before writing.** User explicitly called this out — don't machine-gun edits.
- **Ask, don't act.** User flagged multi-choice situations repeatedly. See global CLAUDE.md rules.
- **No separate installation.md** — fits in README.
- **No separate pages.md** — `Settings_Page` is thin, fits in README.
- **No separate validation.md/repositories.md/layouts.md as individual files** — user wants just the three doc files listed above.
- **"User docs"** = how plugin devs USE the library. Not theme-author HTML contracts. Not contributor internals.
- The old `field-reference.md` was "shit" because it documented CSS classes/HTML output instead of the PHP method API. If anything like it comes back as "contributor docs," it needs clearly signposted audience.

## Recommended next-session plan

1. `git checkout HEAD -- docs/themes.md` to restore.
2. Decide with user: does `docs/themes.md` stay as a separate file, or merge into `docs/layouts-and-themes.md`? User said "a file that covers layout AND themes" — suggests one combined file.
3. Confirm the README outline with user BEFORE writing it. Paste the intended section headings and get approval.
4. Write README.md.
5. Write `docs/fields.md` with one section per field type, shared methods at top. Confirm outline first.
6. Write (or merge into) `docs/layouts-and-themes.md`. Confirm with user whether themes.md stays or merges.
7. Do NOT write anything not in the list above without explicit approval.

## Version / composer state

- Target: **v2.1.0**
- `composer.json` `require`:
  - `php: >=8.0.0`
  - `pinkcrab/perique-framework-core: 2.1.*`
  - `pinkcrab/perique-admin-menu: 2.1.*`
  - `pinkcrab/form-components: 2.1.*`
  - `pinkcrab/collection: ^0.2.0`
  - `respect/validation: ^2.0`
- README must state: **no support for Perique < 2.1.***

## Test / quality state (for the change log entry)

- 648+ unit/integration tests, 100% coverage
- 70+ Playwright e2e tests across 7+ showcase/test pages
- 6 bundled themes
- Full jQuery removal (vanilla JS only)
- Known issues doc for FC upstream: `docs/fc-issue-notification-order.md` — wait, let me check if this still exists

```
ls /media/glynn/2024/devilbox_public/php/htdocs/Perique-Settings-Page/docs/
```

If `fc-issue-notification-order.md` is still there, leave it — it's the issue writeup for the Form Components lib. Not user-facing docs but useful to keep.

## The one thing I'm certain about

**Restore `docs/themes.md` first.** I deleted it without asking. Git has it. `git checkout HEAD -- docs/themes.md` should bring it back.
